# OpenSpec Configuration — price-watch
# Auto-generated by sdd-init | 2026-05-25
# Mode: hybrid (Engram + OpenSpec)

context: >
  MercadoLibre price tracker with Python backend, Playwright/requests scraping,
  Telegram bot (NotifyOffersMc), web UI for managing tracked links, and hourly
  scheduled price checks. New project — no stack files detected yet.

strict_tdd: false
reason: "No test runner detected in project — Python/JS stack TBD after exploration."

testing:
  runner:
    available: false
    reason: "Project is empty — stack and test tooling will be chosen during /sdd-new."
  layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  quality:
    linter: false
    type_checker: false
    formatter: false

phases:
  new: true
  explore: true
  propose: true
  spec: true
  design: true
  tasks: true
  apply: true
  verify: true
  archive: true
